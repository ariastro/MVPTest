package com.example.astronout.gdktest.view.base

interface BaseView<T> {
    //fun showDialog(message: String?)
    fun showProgress() //menampilkan progress
    fun hideProgress() //menyembunyikan progress
}